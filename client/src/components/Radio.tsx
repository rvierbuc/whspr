import React from 'react';
import { Link } from 'react-router-dom';

const Room = () => {


    return (
        <div>
            <button type='button' onClick={()=> {}}>
                Create Channel
            </button>
        </div>
    )
}

export default Room;